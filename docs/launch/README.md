# Launch material

- `brag.mp4` — 22 s launch video (1920×1080), made with the [`/brag`](https://github.com/latent-spaces/brag) skill on Hyperframes. Frame 0 is the poster (`brag.jpg`).
- `share-copy.txt` — caption for the post.
- `brag-plan.md`, `composition-brief.md`, `composition/` — the reproducible Hyperframes project: `cd composition && npx -y hyperframes render --quality delivery`.
- `composition/assets/` (music + SFX) is not committed: it comes from the brag skill bundle (music: ende.app "Happy Beats / Business Moves"; SFX: Kenney). Copy `skills/brag/assets/` from the brag repo to re-render.
- `../video-script.md` — the longer 75 s script with voice-over, for a narrated version.
